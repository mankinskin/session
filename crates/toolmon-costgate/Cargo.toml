[package]
name = "toolmon-costgate"
version = "0.1.0"
edition = "2024"
description = "Model-aware cost gate engine and env/CLI wiring for mcp-toolmon"

[dependencies]
toolmon-policy-api = { path = "../toolmon-policy-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
